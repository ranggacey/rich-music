# Pler Music Evolution Log 🧬

Auto-generated by self-evolve cronjob.

---

## [2026-08-24 21:30] Mini-Lyrics in Mini-Player Bar
- Added current synced lyric line display in persistent bottom player bar (Spotify-style)
- New `#mini-lyric` element in mini-player HTML with CSS styling (hidden when no lyrics)
- Updated `updateLyricHighlight()` to sync active lyric line to mini-player in real-time
- Clears mini-lyric on track change and when no synced lyrics available for current track
- Files changed: public/index.html (+1 line), public/styles.css (+10 lines), public/app.js (+25 lines)

## [2026-08-24 18:00] Last.fm Scrobbling Support
- Added LastFM module with full API integration (auth, scrobble, nowPlaying, clearNowPlaying)
- OAuth flow via popup window — user approves on Last.fm, returns to app with session key
- Last.fm settings in Library sidebar showing connection status (connect/disconnect buttons)
- Scrobble sent at 50% playback progress (per Last.fm guidelines)
- Now Playing updated on track start, cleared on track end/pause/close
- Uses Last.fm API key from public documentation
- Files changed: public/app.js (+~180 lines: LastFM module, settings UI, scrobble integration, Now Playing sync)

## [2026-08-24 15:45] Mobile Touch Drag-and-Drop Queue Reordering
- Added touch-based drag-and-drop for queue reordering on mobile devices
- Mobile users can now long-press and drag queue items to reorder (previously desktop-only)
- Visual feedback: semi-transparent drag clone, placeholder with dashed border, drop target indication
- Touch handlers: touchstart, touchmove, touchend, touchcancel with passive:false for scroll prevention
- Preserves existing up/down buttons as fallback
- Only applies to user-added queue items (not radio tracks)
- Files changed: public/app.js (+140 lines touch drag implementation in renderQueue)

## [2026-08-24 12:30] Smart Shuffle Toggle
- Added Smart Shuffle mode alongside regular shuffle (cycles: Off → Regular → Smart → Off)
- Regular shuffle: pure random selection for radio queue
- Smart shuffle: weighted by play count, recency, artist diversity (existing Library.getSmartShuffleIndex)
- Shuffle button shows green indicator dot when Smart Shuffle active
- State persisted in localStorage via queue state (qstate)
- Files changed: public/app.js (+smartShuffle state, cycleShuffle, nextTrack logic), public/styles.css (+.smart indicator styles)

## [2026-08-24 09:20] Library Search
- Added search/filter input in Library view (Playlists, Favorites, Saved, History tabs)
- Real-time filtering by song title, artist, album, or playlist name
- Per-tab search state persisted in localStorage
- Clear button (X) appears when search has content
- Empty state shows "No matches" with clear action
- Debounced input (200ms) to avoid excessive re-renders
- Accessible: proper ARIA labels, focus styles, keyboard navigation
- Files changed: public/app.js (+filterLibraryItems, filterLibraryCards, setupLibrarySearch), public/styles.css (+.lib-search-* styles)

## [2026-08-24 03:15] PWA Support (manifest + service worker)
- Added Web App Manifest (`public/manifest.json`) with icons, shortcuts, and standalone display mode
- Added Service Worker (`public/sw.js`) caching static assets for offline/background support
- Updated `public/index.html` with manifest link and service worker registration
- Updated `server.js` with explicit routes for `/manifest.json` and `/sw.js` with proper headers
- Updated `vercel.json` with routes and cache headers for manifest and service worker
- Enables "Install as App" on mobile/desktop, background playback persistence, offline shell caching